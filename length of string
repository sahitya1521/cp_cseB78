#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

#include <stdio.h>

int main() {
    char str[1000];
    int length = 0;

    scanf("%s", str);

    while (str[length] != '\0') {
        length++;
    }

    printf("%d", length);

    return 0;
}
